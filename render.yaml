services:
  - type: web
    name: financial-dictionary-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT  # Pure Uvicorn!
    plan: free
